import cv2 as cv

if __name__ == "__main__":
    while(True):
        try:
            img = cv.imread("img.jpg")
            rgb = cv.
        except (cv.error, OpenCV Error):
            print
            "Can't do the thing"
            continue
    print("still going")
    exit(0)